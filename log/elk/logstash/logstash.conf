input {
  redis {
    host => "redis"
    port => 6379
    type => "json"
    key => "logstash"
    data_type => "channel"
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "logstash-%{service_name}-%{+YYYY.MM.dd}"
  }
}